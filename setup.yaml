apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zero-home-data
  labels:
    zero: home-data
spec:
  storageClassName: rook-ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
